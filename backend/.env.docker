# Variables de entorno para Docker
# Base de datos PostgreSQL (usará el contenedor postgres)
DB_NAME=Inventario_db
DB_USER=postgres
DB_PASSWORD=admin123
DB_HOST=postgres
DB_PORT=5432

# JWT
JWT_SECRET=tu_jwt_secret_super_seguro_para_produccion_2024

# Servidor
PORT=3000
NODE_ENV=production